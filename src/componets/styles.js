import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  listingMain: {
    padding: 15,
    borderBottomWidth: 1,
  },
  textStyle: {
    color: 'black',
  },
});

export default styles;
